# Per-connector secrets are created dynamically by the app when a user
# adds a connection (e.g. liveli-cn-<connectorId>). This file declares
# the project-level secrets needed at build/runtime.

# Placeholder: any global Liveli secrets (e.g. Clerk webhook signing key)
# would go here as `google_secret_manager_secret` resources with their
# initial version supplied out-of-band (not in Terraform).
