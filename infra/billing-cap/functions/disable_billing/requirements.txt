# Cloud Functions gen 2 runtime — wraps the CloudEvent handler.
functions-framework>=3.5.0,<4.0.0

# Cloud Billing API client. Provides CloudBillingClient for read/write
# operations on project billing associations.
google-cloud-billing>=1.13.0,<2.0.0

# Structured logging — installs a handler that converts Python logging
# extra={"json_fields": {...}} into Cloud Logging jsonPayload entries.
# Without this, the log-based metric filter on jsonPayload.threshold_crossed
# never matches and alerts never fire.
google-cloud-logging>=3.10.0,<4.0.0
