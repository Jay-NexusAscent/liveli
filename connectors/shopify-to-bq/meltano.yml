version: 1
default_environment: prod
project_id: liveli-shopify-to-bq

environments:
  - name: prod

plugins:
  extractors:
    - name: tap-shopify
      variant: meltanolabs
      pip_url: meltanolabs-tap-shopify
      # Required env:
      #   TAP_SHOPIFY_STORE         (the {store}.myshopify.com handle)
      #   TAP_SHOPIFY_ADMIN_API_KEY (Admin API access token, starts shpat_)
      config:
        api_version: "2024-10"
      select:
        - orders.*
        - products.*
        - customers.*
        - inventory_items.*
        - transactions.*
        - refunds.*
        - locations.*

  loaders:
    - name: target-bigquery
      variant: z3z1ma
      pip_url: git+https://github.com/z3z1ma/target-bigquery.git@v0.7.4
      config:
        method: storage_write_api
        denormalized: true
        upsert: true
